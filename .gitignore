# ── Dipendenze ──
node_modules/

# ── Next.js ──
.next/
out/

# ── Environment ──
.env
.env.local
.env.*.local

# ── Database ──
*.db
*.db-journal
prisma/migrations/

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── Build ──
dist/
build/

# ── Upload ──
public/uploads/avatars/*
!public/uploads/avatars/.gitkeep

# ── Misc ──
*.log
npm-debug.log*
cookies.txt
_test_mcp.cjs
stitch-prompt.md
tsconfig.tsbuildinfo

# ── Deploy artifacts ──
# Il pacchetto standalone generato da `npm run build` + lo script di
# staging. Viene rigenerato ad ogni release, non va versionato.
presenze-hr-deploy*.zip

# ── Claude Code working dir ──
# Configurazione per-user dell'harness CLI, non va versionata.
.claude/

graphify-out/
graphify-out/

# ── Ruflo / claude-flow runtime ──
.claude-flow/
.swarm/
.ruflo-init-backup/

# ── GSD planning state ──
.planning/

# ── Local DB snapshots (prod imports, manual backups) ──
prod_db/
prisma/dev.db.before-*
prisma/dev.db.local-backup-*
