# Release runbook — 2026-07-06

> **Sostituisce integralmente `RELEASE-2026-06-29.md` (mai deployata).** Prod è ancora a `rel-2026-06-22`: questa release è CUMULATIVA (tutto il contenuto della 06-29 + le novità di oggi).

**Cosa rilascia:**
1. **Predittore Ammortamento Ferie** (prima messa in produzione): toggle per-dipendente, distribuzione automatica del residuo sull'anno (spread / week-end lunghi / ponti / rotazione), pagina **Piano ammortamento** con conferma/annulla HR, ricalcolo al volo e all'import paghe.
2. **Grafico andamento monte ferie/permessi** (dashboard + pagina Ferie), in giorni (ROL ore/8), proiezione a fine anno con divisore "oggi".
3. **Accrual ROL corretto a 4h/mese** (48h/anno FT) + predittore che ammortizza il monte **proiettato** a fine anno.
4. **Fix doppio-conteggio carico commercialista**: le ferie/permessi ≤ mese dell'ultimo tabulato sono già nel residuo certificato → non più ri-sottratte (Stefano Brunelli: monte corretto **25,82** invece di 23,82).
5. **[NUOVO 07-06] Rischedulazione giorni predittore**: bottone "Rischedula" sul Piano ammortamento → il giorno viene vietato per sempre (`LeavePredictorExclusion`) e il piano rigenerato altrove; chip "Giorni esclusi" con undo. Rischedulare un giorno **confermato** invia al dipendente l'avviso di annullamento (Telegram + email).
6. **[NUOVO 07-06] Stile viola "ghost"** per le ferie predette (tratteggiato = proposta, pieno = confermata, icona ✦) su calendario, Gantt e piano — al posto del ring ambra + "P".
7. **[NUOVO 07-06] Preview decisionale** sul Piano ammortamento: monte attuale, accumulo mensile, sparkline burn-down con divisore "oggi", giorni raggruppati per mese con accrual e monte a fine mese.

**BUILD_ID:** `0Y63IMPgsgPCZyrvOGvQL`
**Commit:** `1fcc9cf` (tag `rel-2026-07-06`).
**Pacchetto:** `D:\Develop\AI\Hr\presenze-hr-deploy.zip` (83,1 MB) — buildato e validato (server.js root + .next/BUILD_ID + prisma/schema.prisma OK).

---

## ⚠️ SCHEMA DB CAMBIATO — serve `db:push`

Nuovi elementi rispetto a prod/`rel-2026-06-22` (tutti **additivi** → nessuna perdita dati, compatibili col codice vecchio ancora in esecuzione):
- `Employee.leavePredictorEnabled Boolean @default(false)`
- `LeaveRequest.source String @default("MANAGER")`, `confirmedAt DateTime?`, `confirmedById String?`, `@@index([source, confirmedAt])`
- nuova tabella `LeavePredictorRun` (+ `@@index([runAt])`)
- **[NUOVO 07-06]** nuova tabella `LeavePredictorExclusion` (`@@unique([employeeId, date])`, `@@index([employeeId])`)

Essendo **additivo**, il `db:push` si può lanciare **mentre il servizio vecchio gira ancora** (Step 3) → si azzera la finestra di errore "colonna mancante" prima dello swap del codice.

---

## Sequenza (dal SERVER via RDP, PowerShell admin)

### 0. (dal PC dev) push + tag git — GIÀ FATTO
`origin/main` → `1fcc9cf`, tag `rel-2026-07-06`, CI verde.

> **⚠️ `E:\www\hr\deploy` NON ESISTE PIÙ sul server**: `publish-server.ps1` sposta l'intera `E:\www\hr` nel backup datato e non ripristinava `deploy\` (lo zip non conteneva gli script) → la cartella è sparita col publish del 2026-06-22. Gli script sono ancora in `E:\www\hr.bak-*\deploy\`. **Da questa release lo zip include `deploy\`** (fix in `package.ps1`), quindi: estrai il pacchetto (Step 2) e lancia gli script da `E:\HR\incoming\pkg\deploy\` — sono position-independent (path tutti parametrizzati assoluti). Dopo questo publish la cartella `E:\www\hr\deploy` tornerà a esistere da sola.

### 1. Carica il pacchetto sul server
Copia `presenze-hr-deploy.zip` in `E:\HR\incoming` (NON estrarlo a mano in `E:\www\hr`).

### 2. Estrai il pacchetto + backup DB (prima di tutto)
```powershell
Expand-Archive 'E:\HR\incoming\presenze-hr-deploy.zip' -DestinationPath 'E:\HR\incoming\pkg' -Force

E:\HR\incoming\pkg\deploy\backup-db.ps1      # copia prod.db in E:\HR\backups
```
(`publish-server.ps1` rifà comunque il backup WAL-safe, ma qui ci tuteliamo prima del `db:push`.)

### 3. Schema DB — `db:push` additivo (servizio ANCORA attivo, sicuro)
Il CLI `prisma` non è nello standalone → installalo temporaneo e usa lo schema NUOVO dal pacchetto (già estratto allo Step 2).
```powershell
npm install -g prisma@6.19.2

# DATABASE_URL = LO STESSO del servizio (in NSSM, non in .env). Leggilo:
#   & 'C:\ProgramData\chocolatey\bin\nssm.exe' get PresenzeHR AppEnvironmentExtra
# il DB prod è E:\HR\data\prod.db → tipicamente:
$env:DATABASE_URL = 'file:E:\HR\data\prod.db'

prisma db push --schema='E:\HR\incoming\pkg\prisma\schema.prisma'
```
Atteso: "Your database is now in sync with your Prisma schema" — aggiunge colonne/tabelle, **nessun DROP**. Il sito resta su e funzionante (il codice vecchio ignora le colonne nuove).

### 4. Swap del codice (unica finestra di downtime, ~secondi)
```powershell
E:\HR\incoming\pkg\deploy\publish-server.ps1 -ZipPath 'E:\HR\incoming\presenze-hr-deploy.zip'
```
(dal pacchetto, NON da `E:\www\hr\deploy` che non esiste più — vedi nota in alto)
Lo script: valida zip+BUILD_ID → verifica NSSM `AppDirectory` → **backup DB** → **stop** → backup datato di `E:\www\hr` → estrae → **ripristina `.env`, `public\uploads`, `logs`, `iis\`** → **start** → health check → pruning.

Atteso:
```
  Deploy completato
  BUILD_ID nuovo:      0Y63IMPgsgPCZyrvOGvQL
  Backup:              E:\www\hr.bak-<timestamp>
```
Se `throw` PRIMA dello stop (zip/AppDirectory) → sito ancora su, zero downtime → risolvi e rilancia.

### 5. Riallineamento saldi — RE-IMPORT tabulato (OBBLIGATORIO)
Il fix cutoff cambia la formula dell'`adjust`. Gli `adjust` salvati dai vecchi import vanno ricalcolati: **ri-importa l'ultimo tabulato del consulente**.
1. Browser → **https://hr.epartner.it** → `/settings/payroll-import`.
2. Carica l'ultimo `TAB. FERIE ...pdf` → **Anteprima** → **Conferma**.
   - L'import è idempotente (upsert), ricalcola tutti gli `adjust` con la nuova formula e **rigenera automaticamente il piano predittore** per i dipendenti abilitati.

### 6. Verifica
```powershell
Invoke-WebRequest http://127.0.0.1:3100/api/kiosk/health -UseBasicParsing | Select -Expand Content
```
Poi dal browser su **https://hr.epartner.it**:
1. Login → dashboard + griglie caricano.
2. **/leaves → "Per dipendente" → Stefano Brunelli**: monte ferie = **25,82** (non 23,82). Le ferie 8 e 29 maggio NON sono più ri-sottratte.
3. **Grafico "Andamento monte ferie/permessi"** (dashboard e /leaves) si disegna su tutto l'anno con il divisore "oggi".
4. **Predittore**: abilita il toggle su un dipendente test (Modifica dipendente → "Predittore ammortamento ferie") → `/leaves/amortization` → **Ricalcola** → genera giorni.
5. **[NUOVO] Rischedula**: sul piano, clic sull'icona calendario-X di un giorno futuro → conferma → il giorno finisce nei chip "Giorni esclusi" e il piano si rigenera su un'altra data; la ✕ sul chip lo rimuove.
6. **[NUOVO] Stile**: in `/leaves` (calendario e Gantt) i giorni predetti sono viola tratteggiato (✦), quelli confermati viola pieno; legenda aggiornata.
7. **[NUOVO] Preview**: ogni card del piano mostra "Monte attuale", "Accumulo mese", sparkline con "oggi" e i giorni raggruppati per mese con monte a fine mese.

---

## Rollback
Solo CODICE (lo script stampa i comandi esatti a fine run):
```powershell
$nssm = 'C:\ProgramData\chocolatey\bin\nssm.exe'
& $nssm stop PresenzeHR confirm
$bak = Get-ChildItem 'E:\www\hr.bak-*' -Directory | Sort Name -Desc | Select -First 1
Remove-Item 'E:\www\hr' -Recurse -Force
Move-Item $bak.FullName 'E:\www\hr'
& $nssm start PresenzeHR
```
**DB: nessun rollback.** Le aggiunte di schema sono additive — il codice vecchio le ignora, quindi il rollback del solo codice è sicuro. (Per tornare a uno snapshot DB, usa il backup di `E:\HR\backups` fatto allo Step 2.)

---

## Note / trappole (dai precedenti incidenti)
- Mai rename/move a mano di `E:\www\hr`: contiene `iis\` (physicalPath sito IIS) e `public\uploads\`. Lo script li preserva — un move manuale no → 500.19 (incidente 2026-04-17, ~40 min down).
- Config prod vera = NSSM `AppEnvironmentExtra`, non `.env` (vale anche per `DATABASE_URL` allo Step 3).
- Server multi-tenant IIS (~25 siti): non toccare config IIS globale.
- Il `db:push` allo Step 3 è additivo: se per errore venisse rilanciato, è idempotente (nessun cambiamento).
