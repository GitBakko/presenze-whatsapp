# Release runbook — 2026-07-07

> Prod è a `rel-2026-07-06` (deployata ieri, chiusa). Questa release è **solo codice, NESSUN cambio schema, NESSUN db:push, NESSUN re-import**: `publish-server.ps1` da solo è l'intero deploy.

**Cosa rilascia:**
1. **Elimina piano predittore con un click** (`/leaves/amortization`): bottone header "Elimina piano" (tutti i dipendenti) + "Elimina tutti" su ogni card (singolo dipendente). Elimina tutti i giorni **futuri** generati dal predittore, **confermati inclusi**; i giorni passati restano. Il dipendente riceve l'avviso di annullamento (Telegram + email) **solo per i giorni confermati** (le proposte non confermate non erano mai state annunciate).
2. **Filtro per dipendente** sul Piano ammortamento (select "Tutti i dipendenti / …" nell'header).
3. **Le richieste umane sovrascrivono SEMPRE le ferie predette**: una richiesta ferie/permesso che interseca giorni del predittore non riceve più `OVERLAP_BLOCK` — i giorni predetti intersecati (anche confermati) vengono cancellati alla creazione della richiesta. Vale su **tutti i canali**: form admin/portale, API esterna, email-ingest, Telegram (`/ferie`, `/permesso`) e modifica richiesta. Nota: la sovrascrittura avviene anche per richieste PENDING; se poi rifiutate, un "Ricalcola" rigenera i giorni.

**BUILD_ID:** `5ClvwdrUKUR57V90ijvxZ`
**Commit:** feature `070e352`, tag `rel-2026-07-07` → `fdf064f` (feature + runbook, stesso codice del build).
**Pacchetto:** `D:\Develop\AI\Hr\presenze-hr-deploy.zip` (83,1 MB) — buildato e validato (server.js root + .next/BUILD_ID + prisma/schema.prisma OK).

---

## ✅ SCHEMA DB INVARIATO — niente `db:push`

`git diff rel-2026-07-06..HEAD -- prisma/schema.prisma` è vuoto. Salta ogni step Prisma.

---

## Sequenza (dal SERVER via RDP, PowerShell admin)

### 0. (dal PC dev) push + tag git
`origin/main` → `070e352`, tag `rel-2026-07-07`, CI verde (verificare prima di copiare lo zip).

### 1. Carica il pacchetto sul server
Copia `presenze-hr-deploy.zip` in `E:\HR\incoming` (NON estrarlo a mano in `E:\www\hr`).

### 2. Swap del codice (unica finestra di downtime, ~secondi)
Dal publish di ieri `E:\www\hr\deploy` esiste di nuovo (lo zip include gli script):
```powershell
E:\www\hr\deploy\publish-server.ps1 -ZipPath 'E:\HR\incoming\presenze-hr-deploy.zip'
```
Lo script: valida zip+BUILD_ID → verifica NSSM `AppDirectory` → **backup DB** → **stop** → backup datato di `E:\www\hr` → estrae → **ripristina `.env`, `public\uploads`, `logs`, `iis\`** → **start** → health check → pruning.

Atteso:
```
  Deploy completato
  BUILD_ID nuovo:      5ClvwdrUKUR57V90ijvxZ
  Backup:              E:\www\hr.bak-<timestamp>
```
Se `throw` PRIMA dello stop (zip/AppDirectory) → sito ancora su, zero downtime → risolvi e rilancia.

### 3. Verifica
```powershell
Invoke-WebRequest http://127.0.0.1:3100/api/kiosk/health -UseBasicParsing | Select -Expand Content
```
Poi dal browser su **https://hr.epartner.it**:
1. Login → dashboard carica.
2. **`/leaves/amortization`**: in header compaiono la **select filtro dipendente** e il bottone rosso **"Elimina piano"**; su ogni card il bottone **"Elimina tutti"**.
3. **Filtro**: seleziona un dipendente → resta visibile solo la sua card.
4. **Elimina (test su un dipendente)**: "Elimina tutti" sulla card → dialog con conteggio giorni futuri/confermati → conferma → card svuotata, toast "N giorni del predittore eliminati". Poi **Ricalcola** per rigenerare il piano.
5. **Sovrascrittura**: da `/leaves` crea una richiesta ferie admin su un periodo che copre un giorno predetto → **nessun errore di conflitto**, il giorno predetto sparisce dal piano e resta la richiesta reale.

---

## Rollback
Solo CODICE (lo script stampa i comandi esatti a fine run):
```powershell
$nssm = 'C:\ProgramData\chocolatey\bin\nssm.exe'
& $nssm stop PresenzeHR confirm
$bak = Get-ChildItem 'E:\www\hr.bak-*' -Directory | Sort Name -Desc | Select -First 1
Remove-Item 'E:\www\hr' -Recurse -Force
Move-Item $bak.FullName 'E:\www\hr'
& $nssm start PresenzeHR
```
**DB: nessun rollback necessario** (nessun cambio schema; le cancellazioni fatte via UI sono azioni utente, non del deploy).

---

## Note / trappole (dai precedenti incidenti)
- Mai rename/move a mano di `E:\www\hr`: contiene `iis\` (physicalPath sito IIS) e `public\uploads\`. Lo script li preserva — un move manuale no → 500.19 (incidente 2026-04-17, ~40 min down).
- Config prod vera = NSSM `AppEnvironmentExtra`, non `.env`.
- Server multi-tenant IIS (~25 siti): non toccare config IIS globale.
