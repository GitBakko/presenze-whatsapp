# Release runbook — 2026-06-15

**Cosa rilascia:** fix Revisione Presenze + ferie weekend + saldo ferie per dipendenti senza schedule.
**BUILD_ID:** `qF0klrEVkvQ6bHYjTrDm5`
**Commit:** `44c02ea` (push su `origin/main`, attendi CI verde).
**Pacchetto:** `D:\Develop\AI\Hr\presenze-hr-deploy.zip` (82.7 MB) — già buildato e verificato (server.js root + .next/BUILD_ID + prisma/schema.prisma OK).

---

## ✅ NESSUNA modifica allo schema DB

A differenza della 2026-06-10, questa release è **solo codice**: nessuna colonna/tabella nuova, nessun `db:push`. `publish-server.ps1` non tocca il DB. Niente da fare lato schema.

Cosa cambia:
- `excel-presenze.ts` — fallback Lun-Ven per dipendenti FULL_TIME senza `EmployeeSchedule` (prima erano invisibili nella griglia Revisione Presenze); `buildLeaveDayMap` salta weekend/festivi (niente ferie su sab/dom nella mappa interna).
- `leaves/balance.ts` — stesso fallback → il saldo ferie smette di scalare 0 giorni per i senza-schedule.
- `api/leaves/preview-days/route.ts` — riusa la regola condivisa `lib/employees/schedule-fallback.ts`.

---

## Sequenza (eseguire dal SERVER via RDP, PowerShell admin)

### 0. (dal tuo PC) push + tag git
```powershell
cd d:\Develop\AI\Hr
git push origin main
git tag -a rel-2026-06-15 -m "Fix presenze schedule-less + weekend ferie + balance (BUILD qF0klrEVkvQ6bHYjTrDm5)"
git push origin rel-2026-06-15
```
Attendi la CI verde su `origin/main` prima di procedere sul server.

### 1. Carica il pacchetto sul server
Copia `presenze-hr-deploy.zip` in staging, es. `E:\deploy\`.
(NON estrarlo a mano in `E:\www\hr` — ci pensa lo script.)

### 2. (consigliato) Backup del DB di produzione (restore point)
```powershell
cd E:\www\hr\deploy
.\backup-db.ps1
# WAL-safe: copia anche -wal/-shm se presenti (servizio ancora attivo)
$ts = Get-Date -Format 'yyyyMMdd-HHmmss'
Get-ChildItem 'E:\HR\data\prod.db-wal','E:\HR\data\prod.db-shm' -ErrorAction SilentlyContinue |
  ForEach-Object { Copy-Item $_.FullName "E:\HR\backups\$($_.Name).$ts" }
```
> Solo igiene/restore point: questa release non modifica il DB, quindi non serve rollback DB.

### 3. Swap del codice (UNICA finestra di downtime, ~secondi)
```powershell
cd E:\www\hr\deploy
.\publish-server.ps1 -ZipPath 'E:\deploy\presenze-hr-deploy.zip'
```
Lo script (hardened v3): valida zip+BUILD_ID → verifica NSSM `AppDirectory` allineato → **stop** → backup datato di `E:\www\hr` → estrae lo zip → **ripristina dal backup `.env`, `public\uploads`, `logs`, e `iis\` (web.config del sito IIS!)** → **start** → health check con retry → pruning vecchi backup.

Output finale atteso:
```
  Deploy completato
  BUILD_ID nuovo:      qF0klrEVkvQ6bHYjTrDm5
  Backup:              E:\www\hr.bak-<timestamp>
```
Se lo script fa `throw` PRIMA dello stop (zip non valido / AppDirectory disallineata) → **sito ancora su, zero downtime** → risolvi e rilancia.

### 4. Verifica
```powershell
Invoke-WebRequest http://127.0.0.1:3100/api/kiosk/health -UseBasicParsing | Select -Expand Content
```
Poi dal browser su **https://hr.epartner.it**:
1. Login → la dashboard carica.
2. **/presenze** (Revisione Presenze), mese **maggio 2026** → la griglia ora mostra **tutti** i dipendenti con ore/assenze (non solo Claudio Loreto + Lorenzo Mengana).
3. **Giovanni Fabrizi** → le sue ferie venerdì→domenica mostrano "-" su sab/dom (niente ferie sul weekend); i giorni feriali sono valorizzati.
4. **Dipendenti → Giovanni → saldo ferie** → i giorni ferie usati coincidono con quanto mostrava il popup di richiesta.

---

## Rollback
Lo script stampa i comandi esatti a fine run. In sintesi:
```powershell
$nssm = 'C:\ProgramData\chocolatey\bin\nssm.exe'
& $nssm stop PresenzeHR confirm
$bak = Get-ChildItem 'E:\www\hr.bak-*' -Directory | Sort Name -Desc | Select -First 1
Remove-Item 'E:\www\hr' -Recurse -Force
Move-Item $bak.FullName 'E:\www\hr'
& $nssm start PresenzeHR
```
**Il DB NON va rollbackato** — questa release non lo tocca.

---

## Follow-up dati (NON bloccante per il deploy)
Il fix usa un fallback Lun-Ven 8h per i dipendenti FULL_TIME senza `EmployeeSchedule`. È corretto per ferie e presenze, **ma non ha orari di inizio/fine** → i **ritardi** non si calcolano e gli straordinari usano soglia piatta 8h. Per accuratezza, configura gli orari reali dei 7 dipendenti senza schedule in **Impostazioni > Orari** (`/settings/schedule`).

## Note / trappole (dai precedenti incidenti)
- Mai rename/move a mano di `E:\www\hr`: contiene `iis\` (physicalPath sito IIS) e `public\uploads\`. Lo script li preserva — un move manuale no → 500.19 (incidente 2026-04-17, ~40 min down).
- Config prod vera = NSSM `AppEnvironmentExtra`, non `.env`.
- Server multi-tenant IIS (~25 siti): non toccare config IIS globale.
