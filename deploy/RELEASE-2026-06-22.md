# Release runbook — 2026-06-22

**Cosa rilascia:** Revisione Presenze — (1) evidenzia in giallo le caselle dove ore lavorate + ferie/ROL superano il massimo giornaliero (8 FT / 4 PT), mostrando il totale **reale** (non quello cappato dal report); (2) doppio-click su una casella apre un popup per modificare **timbrature E assenze** del giorno (ferie/ROL/malattia), con split automatico delle assenze multi-giorno.
**BUILD_ID:** `hMPZiJjF_eC77mk0L_iOe`
**Commit:** `442c108` (tag `rel-2026-06-22`, CI verde su `origin/main`).
**Pacchetto:** `D:\Develop\AI\Hr\presenze-hr-deploy.zip` (82.7 MB) — buildato e validato (server.js root + .next/BUILD_ID + prisma/schema.prisma OK).

---

## ✅ NESSUNA modifica allo schema DB

Release **solo codice**: nessuna colonna/tabella nuova, nessun `db:push`. Salta lo step schema; `publish-server.ps1` da solo è l'intero deploy.

Cosa cambia (file):
- `lib/presenze/classify.ts` — nuovi campi `rawEffectiveHours`/`dailyCapHours`/`exceedsDailyCap` su `DayClassification`; la cella si accende gialla anche quando il totale stampato è "ok" ma la somma reale supera il cap. **Report xlsx invariato** (byte-identico: `isReportRed/isReportYellow` non toccati).
- `lib/excel-presenze.ts` — calcola la somma **non cappata** solo nei giorni con un'assenza (l'overtime puro non viene mai segnalato).
- `lib/presenze/issues.ts` — i giorni in eccesso compaiono nel pannello Incoerenze.
- `app/(dashboard)/presenze/page.tsx` — doppio-click su qualsiasi casella → popup; sezione Assenze modificabile.
- **Nuovi:** `lib/leaves/day-edit.ts` (split assenze + op transazionali), `api/presenze/review/day/leave/route.ts` (POST add/editDay/removeDay, admin-only), `GET /api/presenze/review/day` (timbrature + assenze del giorno).

---

## Sequenza (eseguire dal SERVER via RDP, PowerShell admin)

### 0. (dal PC dev) push + tag git — GIÀ FATTO
`origin/main` → `442c108`, tag `rel-2026-06-22`, CI verde. Niente da fare.

### 1. Carica il pacchetto sul server
Copia `presenze-hr-deploy.zip` in `E:\HR\incoming` (NON estrarlo a mano in `E:\www\hr`).

### 2. Swap del codice (UNICA finestra di downtime, ~secondi)
```powershell
cd E:\www\hr\deploy
.\publish-server.ps1 -ZipPath 'E:\HR\incoming\presenze-hr-deploy.zip'
```
Lo script (hardened v3): valida zip+BUILD_ID → verifica NSSM `AppDirectory` allineato → **backup DB** (`prod.db` + `-wal`/`-shm` → `E:\HR\backups`, servizio ancora attivo) → **stop** → backup datato di `E:\www\hr` → estrae lo zip → **ripristina dal backup `.env`, `public\uploads`, `logs`, `iis\`** → **start** → health check con retry → pruning.

Output atteso:
```
  Deploy completato
  BUILD_ID nuovo:      hMPZiJjF_eC77mk0L_iOe
  Backup:              E:\www\hr.bak-<timestamp>
```
Se `throw` PRIMA dello stop (zip non valido / AppDirectory disallineata) → **sito ancora su, zero downtime** → risolvi e rilancia.

### 3. Verifica
```powershell
Invoke-WebRequest http://127.0.0.1:3100/api/kiosk/health -UseBasicParsing | Select -Expand Content
```
Poi dal browser su **https://hr.epartner.it** → **/presenze** (Revisione Presenze), mese con dati (es. **maggio 2026**):
1. Login → dashboard carica; la griglia carica.
2. **Doppio-click** su una casella qualsiasi → si apre il popup con sezioni **Timbrature** e **Assenze**.
3. Se esiste un giorno con timbrature **e** un'assenza (es. ROL) che insieme superano il cap → la casella è **gialla** e mostra il totale reale (es. `10`); il popup mostra il banner ambra "ore lavorate + assenze … oltre il massimo".
4. (correzione reale, opzionale) Nel popup riduci le ore di un ROL o rimuovi una timbratura errata → **Salva** → la casella aggiorna colore/valore senza errori. Su una ferie multi-giorno: compare "Copre dal X al Y" e "Rimuovi giorno" spezza solo quel giorno.

> Nota: il popup scrive su dati di produzione (timbrature/assenze reali). Usalo per le correzioni che servono davvero; le letture (colori, apertura popup, banner) sono innocue.

---

## Rollback
Lo script stampa i comandi esatti a fine run. In sintesi:
```powershell
$nssm = 'C:\ProgramData\chocolatey\bin\nssm.exe'
& $nssm stop PresenzeHR confirm
$bak = Get-ChildItem 'E:\www\hr.bak-*' -Directory | Sort Name -Desc | Select -First 1
Remove-Item 'E:\www\hr' -Recurse -Force
Move-Item $bak.FullName 'E:\www\hr'
& $nssm start PresenzeHR
```
**Il DB NON va rollbackato** — questa release non lo tocca (le op del popup sono additive/auditate, ma il deploy in sé non altera lo schema).

---

## Note / trappole (dai precedenti incidenti)
- Mai rename/move a mano di `E:\www\hr`: contiene `iis\` (physicalPath sito IIS) e `public\uploads\`. Lo script li preserva — un move manuale no → 500.19 (incidente 2026-04-17, ~40 min down).
- Config prod vera = NSSM `AppEnvironmentExtra`, non `.env`.
- Server multi-tenant IIS (~25 siti): non toccare config IIS globale.
